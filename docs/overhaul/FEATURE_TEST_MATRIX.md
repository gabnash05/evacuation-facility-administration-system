# Feature and Test Matrix

| Feature | Frontend entry points | Backend entry points | Persistence | Existing automated tests | Current audit state |
| --- | --- | --- | --- | --- | --- |
| Authentication and RBAC | Login, protected routes, persisted auth store, role routes | `/auth/login`, `/auth/register`, `/auth/logout`, `/auth/me` | `users` | none found | Login validates credentials and returns `{role, token}` while setting an `access_token` cookie; JWT accepts headers and cookies. The client persists identity/role but does not hydrate `/auth/me` before routing, and it attempts to read the HTTP-only cookie despite also retaining the JSON token in Axios memory (FRONTEND-001). UI route roles differ from navigation and center helper field casing (FRONTEND-002). Server enforcement remains SECURITY-001/002/003. |
| User management | City/center admin user pages | `/users/**` | `users`, `evacuation_centers` | `test_user_route_guards.py`: JWT guard and invalid-page-before-service behavior | List/detail/create/update/delete/deactivate/reactivate require a JWT but have no actor authorization. Any authenticated role can access them. Role/center update invariant is incomplete; SECURITY-002, CONTRACT-002, DATABASE-001. TEST-001C establishes only no-database boundary coverage; actor/role/center allow/deny coverage is required before SECURITY-002 is complete. |
| Evacuation centers | City centers page, map components | `/evacuation_centers/**` | `evacuation_centers` | none found | review pending |
| Events | City dashboard/event components | `/events/**` | `events`, `event_centers` | none found | review pending |
| Households and individuals | role household pages | `/households/**`, `/individuals/**` | `households`, `individuals` | `test_household_model.py`: household detail preserves distinct household and joined-center fields; authorization/schema/service regressions cover selected request contracts | Household detail no longer loses household address/timestamps to joined center columns (BACKEND-005 portion fixed). Individual pagination/age-boundary consistency and household-update runtime validation remain open. |
| Attendance and transfers | Attendance V1/V2 pages, check-in/out/transfer modals (frontend audit in progress) | `/attendance/**` | `attendance_records`, `individuals`, `events`, `evacuation_centers` | none found | Check-in, checkout, transfer, history, center/event lists, and batch APIs are implemented. Singleton checkout treats a record ID as an individual ID (ATTENDANCE-001); transfer and batches are multi-commit/non-atomic (ATTENDANCE-002); individual history and recorder attribution lack safe server ownership (SECURITY-007); contracts/time handling are hand-built and inconsistent (CONTRACT-005). The client uses record IDs correctly but incurs failed validation/active-event preflight calls before its existing-center-route fallback; batch check-in assigns every item the first center's event without a shared-center check (FRONTEND-008). |
| Aid allocation/distribution | City/center allocation pages; city/center/volunteer distribution pages; allocation/distribution modals, tables, Zustand stores | `/allocations/**`, `/aid-categories`, `/distributions/**` | `aid_categories`, `allocations`, `distribution_sessions`, `distributions` | `test_aid_allocation_update.py` and `test_aid_allocation_model.py`: service/model field allowlists, server-derived quantity, and malicious-identifier cases; `EditAllocationForm.test.tsx`: no browser-derived remaining quantity | Allocation update is now fixed-field and server-authoritative at both service and model boundaries (resolved SECURITY-009 / allocation portion of FRONTEND-006). Distribution creation/status actions still lack actor/center/role enforcement (SECURITY-008), bypass guarded DB functions, hard-code event 1, and commit partially (DISTRIBUTION-001). Client exposes three non-existent allocation endpoints, has conflicting status/record types, and a city distribute action is a no-op (FRONTEND-005/007). |
| Dashboard statistics | City/center/volunteer dashboards; shared `ErrorAlert` | `/stats/**` | aggregate queries | `ErrorAlert.test.tsx`: empty-error and alert-semantics cases | Role dashboards share stats, map, and event display flows. `ErrorAlert` now exposes displayed failures with `role="alert"` (TEST-001B / resolved FRONTEND-019). Aggregate/individual filter and response contracts remain CONTRACT-003; dashboard accessibility/privacy work remains FRONTEND-012. |

The completed matrix will add expected behavior, role permissions, contract sources, external dependencies, test gaps, findings, and ticket IDs.
