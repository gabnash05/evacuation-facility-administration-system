# Findings Register

> Evidence status: entries below are confirmed by static inspection or executed baseline commands. Remediation tickets remain pending complete audit unless marked otherwise.

| ID | Severity | Subsystem | Finding | Evidence | Status |
| --- | --- | --- | --- | --- | --- |
| DEVOPS-001 | P1 | Production runtime | The configured Gunicorn target could not start because `backend/wsgi.py` did not expose `app`. | Fixed 2026-09-03: `from wsgi import app` now succeeds and registers 84 routes; Black/isort checks pass. | resolved |
| SECURITY-001 | P1 | Configuration/auth | The base configuration contains fallback secret values, `DEBUG = True`, insecure JWT cookie defaults, and disabled cookie CSRF protection. | `backend/app/config.py`; auth route inspection. | open |
| SECURITY-002 | P1 | Authorization | Every `/api/users/**` route previously accepted any valid JWT without resolving the actor or enforcing role/center scope. | Fixed 2026-09-04: every endpoint resolves the persisted active actor; super admins manage all users, city admins manage lower roles, center admins manage volunteers at their own center, and volunteers have no access. List queries bind allowed roles and center scope; self-management through generic endpoints is rejected. | resolved |
| SECURITY-003 | P2 | Authentication/privacy | Login returns the JWT in JSON and also sets it as a cookie. The login cookie hard-codes `secure=False`, overriding configuration; cookie-auth CSRF protection is disabled. User-service registration/creation still log email addresses. | Fixed login-log portion 2026-09-04: authentication logging no longer includes the submitted email; a focused route regression proves this. Remaining evidence: `backend/app/{routes/auth.py,services/user_service.py,config.py}`. | partially resolved — token-transport decision required |
| DEPENDENCY-001 | P1 | Supply chain | `npm ci` reported 1 critical, 14 high, 4 moderate, and 1 low advisory. | Executed `npm ci`; advisory identities still require triage. | open |
| TEST-001 | P1 | Regression protection | The repository lacked test files or configured test commands. | Resolved foundation 2026-09-03: backend discovery/WSGI and isolated API-boundary tests plus frontend Vitest/jsdom component tests now pass. Database integration is TEST-002 after DATABASE-002; critical behavior coverage remains mandatory in each owning remediation ticket. | resolved — follow-on coverage is ticket-scoped |
| QUALITY-001 | P2 | Frontend | Formatting and lint gates fail broadly. | Prettier: 162 files; ESLint: 909 errors, 168 warnings. | open |
| QUALITY-002 | P2 | Backend | Formatting, import sorting, and Flake8 gates fail broadly. | Black: 30 files; isort: 32 files; Flake8: 1,211 findings. | open |
| DEVOPS-002 | P2 | Build/dev workflow | Documentation mentions Docker, but no Docker or CI configuration is present. | README and repository inventory. | open |
| BACKEND-001 | P2 | Configuration | Flask-CORS logged an unknown `allow_credentials` option during application creation. | Fixed 2026-09-04: removed the unsupported option while retaining `supports_credentials`; factory regression now asserts no Flask-CORS warning. | resolved |
| DEVOPS-003 | P2 | Contributor workflow | Contributor guides describe Atlassian/Jira/Bitbucket workflows although the configured remote is GitHub; branch-target and hosting guidance conflict. | `docs/README.md`, `CONTRIBUTING.md`, Git remote inspection. | open |
| DEVOPS-004 | P2 | Reproducibility | Node runtime is not pinned and the Pipfile uses wildcard direct dependencies; the lockfiles are current but toolchain policy is incomplete. | `frontend/package.json`, `frontend/package-lock.json`, `backend/Pipfile*`. | open |
| DEVOPS-005 | P2 | Git hooks | The frontend Husky prepare script cannot locate the parent repository `.git` from `frontend/`; its tracked hook also depends on an untracked Husky helper path. | Executed `npm ci`; `frontend/.husky/pre-commit`. | open |
| CONTRACT-001 | P2 | Environment configuration | Backend example configuration advertises `/api/v1`, while Flask registers routes under hard-coded `/api`; `API_PREFIX` is not consumed by the base configuration. | `backend/.env.example`, `backend/app/__init__.py`, `backend/app/config.py`. | open |
| CONTRACT-002 | P2 | User contracts | User update validation previously verified role/center consistency only when `role` was supplied, allowing an invalid persisted combination through a center-only patch. The list route documented `center_id` but read untyped `centerId`. | Fixed 2026-09-04: user updates now validate the merged persisted/requested role-center state before authorization/write; the list route accepts typed documented `center_id` while retaining `centerId` compatibility. Focused service/route regressions and the aggregate backend suite pass. | resolved |
| FRONTEND-021 | P2 | Client authorization contract | `useAuth().canAccessCenter` read non-existent `user.centerId` while the canonical frontend/backend user contract exposes `center_id`, denying valid lower-role center access. | Fixed 2026-09-04: the hook uses canonical `center_id`; a focused hook regression proves own-center allow and other-center deny behavior. | resolved |
| DATABASE-001 | P2 | Persistence | User write methods commit independently and do not roll back on exceptions. The generic update method builds SQL column names dynamically from its caller-provided dictionary, and list queries select `password_hash` although the response schema omits it. | `backend/app/models/user.py`; `backend/app/services/user_service.py`. | open |
| DATABASE-002 | P1 | Schema lifecycle | Flask-Migrate is initialized, but no tracked migration chain exists. `setup_db.py` executes one monolithic PostgreSQL schema script; its `CREATE TABLE IF NOT EXISTS` statements do not make subsequent `ALTER TABLE ... ADD CONSTRAINT` or `CREATE TRIGGER` operations rerunnable, and its duplicate-table-only error handling cannot recover. | `backend/app/models/__init__.py`; `backend/database/setup_db.py`; `backend/database/sql/create_tables.sql`. | open — blocks database migration work |
| DATABASE-003 | P1 | Runtime/database compatibility | The default application URL is SQLite, while the actual schema and queries require PostgreSQL/PostGIS features, including `SERIAL`, `POINT`, PL/pgSQL functions/triggers, `ILIKE`, `RETURNING`, `NOW()`, `AGE()`, `ANY()`, and spatial functions. The schema defines `coordinates POINT`, whereas the center model declares PostGIS `Geometry`. | `backend/app/config.py`; `backend/database/sql/create_tables.sql`; `backend/app/models/evacuation_center.py`; other persistence models. | open — blocks clean-environment validation |
| DATABASE-004 | P2 | Seed/recovery | The sample-data runner splits SQL on semicolons, continues after statement failures, and commits partial results. Its seed SQL uses hard-coded serial IDs and `ON CONFLICT DO NOTHING` on tables without matching natural-key constraints, so it is neither deterministic nor safely rerunnable. | `backend/database/seed_db.py`; `backend/database/sql/seed_db.sql`. | open |
| DATABASE-005 | P2 | Database safety | Setup defaults include a PostgreSQL password and create a database by interpolating `DB_NAME` into DDL without validating an identifier. The script is interactive but has no explicit destructive-operation guard or documented recovery path. | `backend/database/setup_db.py`. | open |
| SECURITY-004 | P1 | Authorization | Evacuation-center routes previously accepted any valid JWT without actor scope. | Fixed 2026-09-04: persisted actors are resolved; direct reads are citywide only for super/city admins and center-scoped for lower roles; citywide/map reads are denied to lower roles; mutation is super-admin-only. | resolved |
| BACKEND-002 | P2 | Event/center integrity | Event creation commits the event before adding centers; subsequent association/capacity operations commit separately. Event update removes/adds center associations but loses `center_ids` before the service checks whether to recalculate capacity. Removing all centers deletes associations before selecting them, so it cannot deactivate now-unassociated centers. | `backend/app/models/event.py`; `backend/app/services/event_service.py`. | open |
| BACKEND-003 | P2 | Event lifecycle | The event-create route rejects every new event whenever an active event exists, even if the requested new event is `monitoring` or `resolved`; the model itself only restricts creation of another active event. Event resolution performs database-triggered and route-level center deactivation plus per-record checkout work in separate commits. | `backend/app/routes/events.py`; `backend/app/models/event.py`; `backend/database/sql/create_tables.sql`. | open |
| BACKEND-004 | P2 | Center lifecycle | Center deletion can cascade-delete households and dependent records. It attempts automatic user reassignment/role mutation inside a raw-SQL path; one branch uses `#` (not PostgreSQL comment syntax) and the policy is undocumented. | `backend/app/models/evacuation_center.py`; `backend/database/sql/create_tables.sql`. | open — destructive-policy decision required |
| CONTRACT-003 | P2 | API contracts | Event and center responses are assembled differently by route/service/model; schemas omit fields later appended by services. Statistics validation applies to the aggregate endpoint but not the three individual endpoints. | `backend/app/{schemas,services,routes}/{event,evacuation_center,stats}*`. | open |
| SECURITY-005 | P2 | Upload handling | Center photo upload enforces only a byte limit; it does not validate MIME type, image decode, dimensions, or storage/response policy, then stores arbitrary base64 data in the database. | `backend/app/services/evacuation_center_service.py`; `backend/app/routes/evacuation_centers.py`. | open |
| SECURITY-006 | P1 | Authorization/privacy | All household and individual routes authenticate but never resolve or authorize the actor. Any valid account can enumerate/search personal data across centers, create/update/delete households or individuals, and invoke global status recalculation. | `backend/app/routes/households.py`; `backend/app/routes/individuals.py`. | open — blocks household/individual changes |
| CONTRACT-004 | P2 | Household/individual contracts | The standalone individual route documents `household_id`, but its create schema does not declare it; the model requires it. The update schema inherits required create fields despite the route documenting a partial update. Household schemas are not used by the route/service mutation paths. | `backend/app/{schemas,services,routes}/{individual,household}*`. | open |
| BACKEND-005 | P2 | Household/individual correctness | Individual list and count apply different center filters, producing incorrect pagination. Age-group categories differ between individual queries (`child` <18) and dashboard stats (`Child` <=12). Household detail selects conflicting column labels from joined tables. | `backend/app/models/{individual,household,stats}.py`. | open |
| DATABASE-006 | P2 | Transaction/data integrity | Individual create/update/delete rely on the service to commit, while status recalculation executes a write-returning database function without committing. Multi-record household edits are transactional, but their individual ownership/head invariants are not independently validated. | `backend/app/models/{individual,household}.py`; `backend/app/services/{individual_service,household_service}.py`. | open |
| ATTENDANCE-001 | P1 | Attendance correctness | The singleton checkout endpoint treats its URL `record_id` as an `individual_id`: the service first loads an individual by that value, and the model searches active attendance by `individual_id` before checking out the resulting record. The route, however, resolves an attendance record and passes its record ID. Checkout therefore fails or checks out the wrong logical target whenever the record and individual IDs differ. | `backend/app/routes/attendance_records.py`; `backend/app/services/attendance_records_service.py`; `backend/app/models/attendance_records.py`. | open — blocks attendance checkout changes |
| ATTENDANCE-002 | P1 | Attendance/transfer integrity | Check-in, checkout, and transfer workflows span multiple independently committed writes. A transfer marks the source record checked out before creating the destination record; automatic transfer during check-in also performs a prior checkout, then separate transfer and check-in writes. Batch operations intentionally return partial results while each item commits independently. Application-level active-record checks are not protected by a transaction or uniqueness constraint. | `backend/app/models/attendance_records.py`; `backend/app/services/attendance_records_service.py`; `backend/app/routes/attendance_records.py`; `backend/database/sql/create_tables.sql`. | open — transfer and batch atomicity policy required |
| SECURITY-007 | P1 | Attendance authorization/privacy | Attendance history for an individual requires only a JWT and has no actor or center-scope check. Several list routes fall back to unrestricted data when the JWT identity has no corresponding user. Check-in, batch check-in, and transfer accept a client-supplied `recorded_by_user_id`, allowing audit attribution spoofing. | `backend/app/routes/attendance_records.py`; `backend/app/services/attendance_records_service.py`. | open — blocks attendance access changes |
| CONTRACT-005 | P2 | Attendance contracts | Attendance has no Marshmallow schema. Routes hand-validate request shapes, accept raw timestamp strings, and return different field sets between list and detail operations. The code uses naive `datetime.now().isoformat()` while schema timestamps are database-naive, leaving timezone and serialization policy undefined. | `backend/app/routes/attendance_records.py`; `backend/app/services/attendance_records_service.py`; `backend/app/models/attendance_records.py`; `backend/database/sql/create_tables.sql`. | open |
| BACKEND-006 | P2 | Attendance lifecycle | Database triggers adjust individual status and occupancy while model/service paths also recalculate or update related lifecycle state. The duplicated responsibilities and differing read guards (for example, current-center evacuees reject inactive centers/events) make behavior dependent on entry path. | `backend/app/models/attendance_records.py`; `backend/app/services/attendance_records_service.py`; `backend/database/sql/create_tables.sql`. | open |
| SECURITY-008 | P1 | Allocation/distribution authorization | Every authenticated user can create a distribution, supplying any household and allocation IDs; no route/service check binds the actor, household, allocation, center, or event together. Any authenticated user can also call the distribution status toggle, which returns or deducts stock, without even resolving the actor. Allocation detail checks center scope only for `center_admin`, so a volunteer can fetch another center's allocation by ID. | `backend/app/routes/distribution.py`; `backend/app/services/distribution_service.py`; `backend/app/routes/aid_allocation.py`. | open — blocks distribution changes |
| SECURITY-009 | P1 | SQL injection | The super-admin allocation update path interpolated every JSON property name into an `UPDATE ... SET` SQL clause. Bound values did not protect the untrusted identifier. | Fixed 2026-09-03: `update_allocation` now accepts a fixed field allowlist, rejects `remaining_quantity` and unknown fields before lookup, and derives remaining stock from persisted totals. `backend/tests/test_aid_allocation_update.py` and the mapped `EditAllocationForm` component regression cover the boundary. | resolved |
| DISTRIBUTION-001 | P1 | Inventory/data integrity | Distribution creation commits the session before each item, bypasses the schema's guarded `create_distribution_session`/`add_distribution_to_session` functions, hard-codes event ID 1, and does not verify allocation/session/household center or event consistency. It can leave partial sessions/items on failure. Distribution update and status toggles mix manual stock changes with separately committing model methods; updating a voided record can adjust stock despite its voided state. | `backend/app/models/distribution.py`; `backend/app/services/distribution_service.py`; `backend/database/sql/create_tables.sql`. | open — inventory integrity and historical-repair policy required |
| CONTRACT-006 | P2 | Allocation/distribution contracts | Allocation create/update use bespoke route parsing rather than a schema. Distribution requires a client `center_id` but ignores it, accepts no event ID, and silently creates sessions with event 1. The distribution history sort schema accidentally concatenates `volunteer_name` and `status`; list, detail, and create allocation responses use different field sets. | `backend/app/routes/aid_allocation.py`; `backend/app/routes/distribution.py`; `backend/app/schemas/distribution.py`; `backend/app/services/{aid_allocation_service,distribution_service}.py`. | open |
| BACKEND-007 | P2 | Allocation/distribution maintainability | Allocation has two `get_center_allocations` definitions (the latter silently replaces the former). Allocation status refresh commits separately after update and the advertised center-stats hook is a no-op. Client control of `remaining_quantity` was removed by SECURITY-009. | `backend/app/services/aid_allocation_service.py`; `backend/app/models/aid_allocation.py`. | partially resolved |
| FRONTEND-001 | P2 | Authentication/route protection | `auth-store` persists `user` and `isAuthenticated` in browser storage, but neither `App`, `ProtectedRoute`, nor `MainLayout` performs an initial server-side identity check. A stale or user-edited persisted role can render protected UI until an API call fails. The API interceptor attempts to extract the HTTP-only `access_token` cookie through `document.cookie`, which cannot succeed for the backend's configured cookie; the login response token is instead held only in Axios memory. | `frontend/src/{services/api.ts,services/authService.ts,store/authStore.ts,hooks/useAuth.ts,components/features/auth/ProtectedRoute.tsx,App.tsx,layouts/MainLayout.tsx}`; `backend/app/routes/auth.py`. | open |
| FRONTEND-002 | P2 | Frontend contracts/navigation | User identity types use API snake_case (`user_id`, `center_id`) but auth helpers and the tracked mock use camelCase (`centerId`, `id`); frontend role/center helpers can therefore make incorrect UI decisions. Route groups intentionally admit extra roles (for example center admins to `/city-admin/*`), while navigation is role-specific; the intended cross-role view policy is undocumented. | `frontend/src/{types/user.ts,hooks/useAuth.ts,hooks/useAuth.mock.ts,App.tsx,components/common/Sidebar.tsx}`. | open — cross-role view policy requires reconciliation |
| FRONTEND-003 | P2 | Accessibility/responsiveness | The shared `DataTable` previously used click-only sortable headers and an empty row without an explanatory state; `MainLayout` used a fixed device-derived `calc(100vw - 21vw)` content width that could conflict with the sidebar. | Fixed 2026-09-04: DataTable sortable headers are native buttons with valid `aria-sort` state and explicit empty state; MainLayout uses responsive flex sizing with `min-w-0`. Mapped component/layout regressions cover both outcomes. | resolved |
| FRONTEND-019 | P2 | Accessibility | A displayed dashboard error lacked alert semantics, so assistive technology could not recognize or announce it as an error. | Discovered by the new `ErrorAlert` component regression in TEST-001B; fixed by adding `role="alert"` to `frontend/src/components/features/dashboard/ErrorAlert.tsx`. | resolved 2026-09-03 |
| FRONTEND-020 | P4 | Frontend build performance | The production build emits a 1.15 MB minified JavaScript chunk and reports that `individualService.ts` is both dynamically and statically imported, so the dynamic imports do not produce a separate chunk. | `npm run build` during TEST-001B; imports from attendance/transfer modals and `store/individualStore.ts`. | backlog — optimize only with route/load-performance evidence |
| FRONTEND-004 | P3 | Frontend maintainability | Shared feedback, toolbar, and pagination patterns are duplicated in feature directories, and there is no React error boundary. | Fixed dead-code portion 2026-09-04: removed the empty `useApi.ts` hook plus unimported `useAuth.mock.ts` and `AuthLayout.tsx`; type-check and production build pass. Remaining evidence: `frontend/src/{components/common/**,components/features/**}`. | partially resolved |
| FRONTEND-005 | P2 | Allocation/distribution client contracts | The allocation client exposes `/allocations/summary`, `/allocations/center/:id/stats`, and `/allocations/bulk`, but no matching backend routes are registered. Distribution client types model statuses as `delivered`/`pending` in one module and `completed`/`voided` in another; history requires a `formatted_items` field the backend does not return. The client bypasses its distribution service for status mutation. | `frontend/src/{services/aidAllocationService.ts,services/distributionService.ts,store/distributionStore.ts,types/aid.ts,types/allocation.ts,types/distribution.ts}`; `backend/app/routes/{aid_allocation,distribution}.py`. | open |
| FRONTEND-006 | P2 | Inventory workflow | The allocation edit form no longer computes or submits `remaining_quantity`; the server derives it from persisted inventory. Distribution UI still exposes void/restore to every rendered role and city/center/volunteer pages share mutable workflow components; this cannot compensate for the missing backend ownership checks. | Fixed allocation portion 2026-09-03: `frontend/src/components/features/aid-allocation/EditAllocationForm.tsx` plus mapped component regression. Remaining distribution concerns: `frontend/src/components/features/distribution/{DistributionHistoryTable,DistributeAidModal,EditDistributionModal}.tsx`; `frontend/src/pages/{city-admin,center-admin,volunteer}/*DistributionPage.tsx`; backend SECURITY-008/DISTRIBUTION-001. | partially resolved |
| FRONTEND-007 | P3 | Allocation/distribution UX | A city allocation-table “Distribute” callback only logs to the console. Client pages repeat the same distribution orchestration, and their status/empty/loading behavior diverges. | `frontend/src/pages/city-admin/CityAdminAidAllocationPage.tsx`; `frontend/src/pages/{city-admin,center-admin,volunteer}/*DistributionPage.tsx`; `frontend/src/components/features/{aid-allocation,distribution}/**`. | open |
| FRONTEND-008 | P2 | Attendance client correctness | Every client check-in and transfer first calls non-existent `/attendance/validate-center/:id` and `/centers/:id/active-event` paths, then falls back to the existing center-events route. This adds avoidable failed requests/error paths before supported mutations. Batch check-in additionally fetches one active event from the first item and applies it to every item without enforcing a common center. | `frontend/src/services/attendanceRecordsService.ts`; `backend/app/routes/{attendance_records,evacuation_centers,events}.py`. | open |
| FRONTEND-009 | P1 | Attendance client correctness | All routed V2 attendance pages construct each table row with `record_id: individual.individual_id`, then pass that value to the checkout and transfer endpoints. Those endpoints are defined around attendance record IDs, and the backend checkout implementation already confuses the two identity domains. A row action can therefore target a different individual or fail as soon as serial ID sequences diverge. | `frontend/src/pages/{city-admin,center-admin,volunteer}/*AttendancePageV2.tsx`; `frontend/src/components/features/attendance/AttendanceTableV2.tsx`; backend finding ATTENDANCE-001. | open — blocks row-action changes |
| FRONTEND-010 | P3 | Attendance maintainability | The application routes only to V2 attendance pages but retains three legacy role pages, legacy table/toolbar components, and a legacy single-transfer modal. The V2 pages duplicate large amounts of orchestration and still contain placeholder deletion handlers that only log success. Consolidate only after preserving documented role scope and row-action behavior. | `frontend/src/pages/{city-admin,center-admin,volunteer}/*AttendancePage.tsx`; `frontend/src/components/features/attendance/{AttendanceTable,AttendanceTableToolbar,TransferModal}.tsx`; `frontend/src/App.tsx`. | open |
| FRONTEND-011 | P2 | Center/event client contracts | Event service/store overwrite every creation status to `active` even though the shared schemas and API model advertise `monitoring` and `resolved`, obscuring the actual lifecycle rule and duplicating the backend’s over-broad active-event gate. The center schema previously accepted unsupported `open`. | Fixed center portion 2026-09-04: the frontend schema now accepts only `active`, `inactive`, or `closed`, matching the backend schema, runtime type, and database constraint; focused Vitest coverage added. Remaining evidence: `frontend/src/{schemas/events.ts,services/eventService.ts,store/eventStore.ts}` and backend BACKEND-003. | partially resolved |
| FRONTEND-012 | P2 | Dashboard accessibility/privacy | Volunteer quick actions were clickable `div`s with no keyboard equivalent or button semantics. Event details additionally logged the full selected event object to the browser console. | Fixed 2026-09-04: quick actions now use labelled native buttons with decorative icons hidden from the accessible name; event details no longer logs selected event data. Focused mapped Vitest regressions prove navigation behavior and no console logging. | resolved |
| FRONTEND-013 | P2 | Household client contracts | Update payloads included nested individuals beyond their Zod contract, and the list client normalized several speculative response shapes rather than sharing a verified contract. The create schema previously omitted required `center_id` and members. | Fixed 2026-09-04: create and update schemas now characterize the complete payload accepted by the current backend update workflow; update types require that full contract; the list service maps only the documented backend pagination response. Focused schema and service regressions cover the request/response boundaries. | resolved |
| FRONTEND-014 | P2 | User-management client contracts | The modal supports password updates. The frontend schema previously omitted the password and did not enforce role/center invariants; the user store logged authenticated response/filter data. | Fixed 2026-09-04: frontend create/update payloads validate role-center invariants and optional password; backend validates merged update state; user-store logging is removed. Focused schema, route/service, and store regressions cover the changes. | resolved |
| FRONTEND-015 | P2 | Individual/statistics contracts | Individual runtime types require `household_id` and use title-case gender values, whereas the exported create Zod schema omitted the household and accepted lowercase gender. The stats schema previously omitted `event_id`. | Fixed 2026-09-04: individual creation now requires a positive household ID, accepts backend/title-case gender values, and makes date of birth optional; stats validates the propagated optional event filter. Focused Vitest regressions cover both schema contracts. | resolved |
| FRONTEND-016 | P3 | Shared client utilities | `formatCapacity` previously divided by zero for a zero-capacity center, and `useDebounce().cancel()` previously read a timeout property that the debounced function never exposed. | Fixed 2026-09-04: `formatCapacity` now emits `0%` for zero total capacity; debounce exposes and uses a real `cancel()` method that clears its pending timer. Focused mapped Vitest regressions cover both outcomes. | resolved |
| SECURITY-010 | P1 | Stored XSS / map rendering | `MonoMap` previously interpolated the database-controlled center name directly into Leaflet `divIcon` HTML and a CSS data attribute. | Fixed 2026-09-04: raw marker HTML contains no center name; React renders the name as tooltip text, and a hostile-name regression asserts the marker HTML cannot contain it. | resolved |
| FRONTEND-017 | P2 | Static map correctness | `StaticMap` previously accepted markers but ignored them, created unused hard-coded placeholder data, and rendered `MonoMap` with no centers. | Fixed 2026-09-04: supplied markers are forwarded to `MonoMap` as centers and placeholder data was removed; a mapped component-boundary regression verifies the forwarded props. | resolved |
| FRONTEND-018 | P3 | Dead/incomplete route source | Profile and settings route files are empty, while the static dashboard sample has no route import and duplicates stale hard-coded product data. | `frontend/src/pages/{shared/profile/ProfilePage,shared/settings/SettingsPage,samples/CityAdminDashboardSample}.tsx`; `frontend/src/App.tsx`. | open |
| PRIVACY-001 | P3 | Documentation | A tracked dashboard screenshot visibly includes a personal email address. | `docs/assets/screenshot-1.png` visual review. | open |

## Finding conventions

Every future finding records: exact evidence, affected behavior, confidence, impacted files, decision requirement, linked ticket, validation, and disposition. P0-P3 findings block their affected ticket; P4 findings are tracked without scope expansion.
